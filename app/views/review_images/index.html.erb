<h1>Listing review_images</h1>

<table>
  <tr>
    <th>Id</th>
    <th>Shop</th>
    <th>User</th>
    <th>Review</th>
    <th>Image path</th>
    <th>Commment</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @review_images.each do |review_image| %>
  <tr>
    <td><%= review_image.id %></td>
    <td><%= review_image.shop_id %></td>
    <td><%= review_image.user_id %></td>
    <td><%= review_image.review_id %></td>
    <td><%= review_image.image_path %></td>
    <td><%= review_image.commment %></td>
    <td><%= link_to 'Show', review_image %></td>
    <td><%= link_to 'Edit', edit_review_image_path(review_image) %></td>
    <td><%= link_to 'Destroy', review_image, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Review image', new_review_image_path %>
