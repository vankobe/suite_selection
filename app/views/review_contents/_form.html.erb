<%= form_for(@review_content) do |f| %>
  <% if @review_content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review_content.errors.count, "error") %> prohibited this review_content from being saved:</h2>

      <ul>
      <% @review_content.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :review_id %><br />
    <%= f.number_field :review_id %>
  </div>
  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
