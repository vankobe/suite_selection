<h1>Listing reviews</h1>

<table>
  <tr>
    <th>User</th>
    <th>Product</th>
    <th>Score</th>
    <th>Country</th>
    <th>Language</th>
    <th>Subject</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @reviews.each do |review| %>
  <tr>
    <td><%= review.user_id %></td>
    <td><%= review.product_id %></td>
    <td><%= review.score %></td>
    <td><%= review.country_id %></td>
    <td><%= review.language_id %></td>
    <td><%= review.subject %></td>
    <td><%= link_to 'Show', review_path(review.id) %></td>
    <td><%= link_to 'Edit', edit_review_path(review.id) %></td>
    <td><%= link_to 'Destroy', delete_review_path(review.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Review', new_review_path %>
