<%= form_for(@shop_review_image) do |f| %>
  <% if @shop_review_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shop_review_image.errors.count, "error") %> prohibited this shop_review_image from being saved:</h2>

      <ul>
      <% @shop_review_image.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :shop_id %><br />
    <%= f.number_field :shop_id %>
  </div>
  <div class="field">
    <%= f.label :review_id %><br />
    <%= f.number_field :review_id %>
  </div>
  <div class="field">
    <%= f.label :image_path %><br />
    <%= f.text_field :image_path %>
  </div>
  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
