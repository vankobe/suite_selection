module SetSeoHash
  def self.included
    include SeoSearch
  end

  def set_seo_hash(options)
    controller_path = options[:controller_path]
    action_name = options[:action_name]
    m = (controller_path + "_" + action_name).gsub(/\//, "_")
    #self.__send__(:m)
  end
end
